void func(char[], int);

int main()
{
    // Function prototype is like defining the type of a function and its arguments, like we did, we set the type of the function at the top and then we defined the function itself at the end.

    return 0;
}

void func(char name[], int age)
{
    // do something.
}